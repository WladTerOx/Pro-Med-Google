# 012 — Закрыть остаток пунктов 001-011, не выполненных в рамках фиксации 002

**Создан:** 2026-08-05 21:10 UTC
**Приоритет:** 🟡 средний
**Связано с:** [`audit/med002`](./../audit/med002-2026-08-05T21-10-00Z-all-fixes-and-ai-fallback-migration.md),
`tasks/{001,003,004,007,008,009,011}`, OpenSpec change `ai-fallback-chain-migration`.

## Контекст

В рамках фиксации `002` бо́льшая часть инфра-фиксов из `tasks/001–011` была
закрыта, но 7 пунктов либо не выполнены, либо выполнены частично.
Эта задача собирает их в один трекабельный список.

## Статус: 🔴 In Progress

| ID | Источник | Задача | Статус |
|---|---|---|---|
| 12.1 | tasks/001 | `<link rel="icon" type="image/x-icon" href="/favicon.ico" />` в `index.html <head>` | ⛔ Open |
| 12.2 | tasks/003 | Обернуть `console.log` в `services/ollama.ts` в `if (import.meta.env.DEV)` (а не раскомментировать) | ⛔ Open |
| 12.3 | tasks/004 | `package.json`: `"preview": "vite --port 4173 --host 0.0.0.0"` → `"preview": "vite preview --port 4173 --host 0.0.0.0"` | ⛔ Open |
| 12.4 | tasks/005 | `deploy.sh` — авто-скрипт: build → sync `dist/` → `docker build` → recreate container | ⛔ Open |
| 12.5 | tasks/007 | Проверить байтом `vite.config.ts` на отсутствие `\t` в строке `allowedHosts: ['med.openaiua.cloud']` | ⛔ Open |
| 12.6 | tasks/008 | Поднять Ollama на `162.19.248.57:11434` (или прислать актуальный `VITE_OLLAMA_BASE_URL`) | ⛔ Open |
| 12.7 | tasks/009 | Пополнить Gemini prepay или прислать новый ключ (`https://aistudio.google.com/projects`) | ⛔ Open |
| 12.8 | tasks/011 | Убрать `|| 'http://192.168.50.250:11434'` из `services/ollama.ts` (требовать `VITE_OLLAMA_BASE_URL`) | ⛔ Open |
| 12.9 | audit/002 | После `npm run build` — синхронизировать `dist/` → `/root/med-proxy/dist/`, пересобрать Docker image, recreate `med-proxy` контейнер | ⛔ Open (шаг deploy) |

## Что проверить перед закрытием

После ручных правок 12.1–12.8:
- [ ] `grep -P '\t' services/*.ts vite.config.ts index.html package.json` — нет TAB
- [ ] `npm run build` → `dist/index*.js` обновился
- [ ] Локальный `vite preview` теперь действительно отдаёт собранный `dist/`
- [ ] В Chrome на `https://med.openaiua.cloud` видна правильная favicon

## Где

- `/root/Projects/Pro-Med-Google/index.html`
- `/root/Projects/Pro-Med-Google/services/ollama.ts`
- `/root/Projects/Pro-Med-Google/package.json`
- `/root/Projects/Pro-Med-Google/vite.config.ts`
- `/root/Projects/Pro-Med-Google/.env` (только для справки, **не** коммитить)
- `/root/Projects/Pro-Med-Google/deploy.sh` (создать)

## Почему отложено

В рамках фиксации 002 фокус был на **AI-fallback миграции** (новое поведение,
документированное через OpenSpec change). Инфра-фиксы из tasks 001-011
включены в коммит частично (что не блокирует деплой AI-fallback), а оставшиеся
пункты собраны сюда, чтобы не потеряться. Это решение согласовано с
пользователем в этой же сессии (`A/C/D/E`).
